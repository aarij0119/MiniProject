<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Registration</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="bg-zinc-900 w-full min-h-screen p-6">
        <div class="text-white">
            <div>
                <img class="w-16 h-16 object-cover rounded-lg" src="/image/uploads/<%= user.profile %>" alt=""> 
                <a href="/file" class="text-green-700" >Upload Profile</a>
            </div>
            <h1 class="text-3xl">Hello,<span class="font-bold"><%= user.username  %></span>👋</h1>
            <p>You can create a post</p>
        </div>  
        <form action="/post" method="post">
            <div class="text-white">
                <textarea name="textarea" class="resize-none w-1/3 mt-4 bg-transparent border rounded-lg pt-4 px-4 text-white"  placeholder="Write a post"></textarea>
                <input value="Create a post" type="submit"  class="block bg-blue-600 px-4 mt-2 py-1 rounded-lg">
            </div>
        </form>
        <div id="posts">
                <h3 class="text-white mt-12 mb-6">Your Posts</h3>
             <div class="flex flex-wrap gap-2">
               <% if(user.posts.length === 0){ %>
                        <h1 class="text-white">No post yet</h1>
              <% }else{ %>
                <% user.posts.reverse().forEach(function(post){ %>
                    <div class="w-1/4 border py-2 px-2  bg-zinc-800 rounded-lg">
                        <h3 class="text-blue-600" >@<%= user.username %></h3>
                        <p class="text-white"><%= post.postadata %></p>
                        <h6 class="text-red-700"><%= post.likes.length %> Likes</h6>
                        <div class="flex items-center justify-between mt-2">
                            <a href="/like/<%= post._id %>" class="text-blue-600">
                                <%= post.likes.indexOf(user._id) ? "Like" : "Unlike" %>
                            </a>
                            <a href="/edit/<%= post._id %>" class="text-yellow-600">Edit</a>
                        </div>
                    </div>
                 <% }) %>
            <%  } %> 
              
             </div>
        </div>
    </div>
</body>
</html>