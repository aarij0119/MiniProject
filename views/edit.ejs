<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Registration</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="bg-zinc-900 w-full min-h-screen p-6">
        <form action="/update/<%= post._id %>" method="post">
            <h3 class="text-white  mb-4">Your Post</h3>
            <div class="text-white">
                <textarea name="textedit" class="resize-none w-1/3 mt-4 bg-transparent border rounded-lg pt-4 px-4 text-white"  placeholder=""><%= post.postadata %></textarea>
                <input value="Update post" type="submit"  class="block bg-yellow-600 px-4 mt-2 py-1 rounded-lg">
            </div>
        </form>
    </div>
</body>
</html>